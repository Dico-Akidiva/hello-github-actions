name: "A workflow for my Hello World file"
on: pull_request
jobs:
  build:
    name: Hello World action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@V1
      - uses: ./action-a
        with: 
          MY_NAME: "Mona" #Configured the error
